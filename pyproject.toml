[tool.ruff]
line-length = 100
target-version = "py39"
src = ["python", "tools"]
exclude = [
    ".venv",
    "python/build",
    "python/.pybuild",
    "python/debian",
]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "PLC0415", "S", "C4", "RUF"]
ignore = [
    "E501",   # line too long — formatter handles this
    "S101",   # assert — invariant checks are intentional and desirable in this codebase
    "S603",   # subprocess with list args — all calls in this codebase use fixed lists
    "S310",   # urllib URL open — all URLs are https:// to known hosts
    "S608",   # SQL injection — no SQL in this codebase; false positive on bash heredoc
    "RUF001", # ambiguous unicode in strings — intentional math notation (×, –, −, σ)
    "RUF002", # same in docstrings
    "RUF003", # same in comments
]

[tool.ruff.lint.per-file-ignores]
"python/tests/conftest.py" = ["E402"]           # sys.path setup must precede local imports
"**/test_*.py" = ["S101", "S106"]               # assert and test credentials are normal in pytest
"tools/tests/*.py" = ["S101", "S106"]           # same for tools test suite
"python/tests/_*.py" = ["S101"]                 # test helpers (not prefixed test_)

[tool.ruff.lint.isort]
known-first-party = ["hokku", "hokku_config", "hokku_setup"]

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
pythonVersion = "3.11"
venvPath = "."
venv = ".venv"
extraPaths = ["python"]
include = [
    "python/hokku",
    "python/tests",
    "tools",
]
exclude = [
    ".venv",
    "python/build",
    "python/.pybuild",
    "python/debian",
]
typeCheckingMode = "basic"

[tool.coverage.run]
source = ["python/hokku"]
omit = [
    "python/hokku/webserver/dither_unconstrained_numba.py",
    "python/hokku/webserver/dither_streaming_numba.py",
]

[tool.coverage.report]
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "\\.\\.\\.",
    "raise NotImplementedError",
]
