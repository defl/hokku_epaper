"""Hokku — e-paper image server and screen flasher.

Subpackages:
- ``hokku.webserver`` — the Flask image server (the ``hokku-server`` app).
- ``hokku.screens`` — per-screen support libraries (e.g. ``hokku.screens.epf1301``).
"""
