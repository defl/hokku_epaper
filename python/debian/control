Source: hokku-server
Section: net
Priority: optional
Maintainer: Dennis Fleurbaaij <mail@dennisfleurbaaij.com>
Build-Depends: debhelper-compat (= 13), python3, dh-python, python3-setuptools, pybuild-plugin-pyproject
Standards-Version: 4.6.0

Package: hokku-server
Architecture: all
Depends: ${python3:Depends}, ${misc:Depends},
 python3-flask,
 python3-waitress,
 python3-pil,
 python3-numpy,
 python3-psutil,
 python3-pip,
 python3-zstd,
 python3-serial,
 python3-defusedxml,
 python3-packaging
# Note: numba, pillow-heif, opencv-python-headless, esptool, and
# esp-idf-nvs-partition-gen are not available as Debian packages (or lack
# required modules). They are installed automatically via pip by the postinst
# script. python3-serial (pyserial) provides serial-port enumeration for the
# screen-flashing feature.
#
# ${python3:Depends} MUST stay here: dh_python3 only processes a package (byte-
# compile + install the module into dist-packages) when the package is named
# python3-* or carries this substvar. Removing it makes dh_python3 skip the
# package entirely, so the hokku module never ships and the service dies with
# "No module named 'hokku'". To stop dh_python3 from turning pyproject.toml's
# pip-only deps (esptool, numba, ...) into apt Depends — esptool maps to a Debian
# package absent from many apt sources, making the .deb uninstallable — we pass
# --no-guessing-deps in debian/rules. The manual list below is authoritative.
Description: Spectra 6 e-ink image server for Hokku/Huessen frames
 Converts and serves images for Hokku e-paper frames using
 Floyd-Steinberg dithering to the 6-color Spectra 6 palette.
 Supports JPEG, PNG, WebP, HEIC, and other image formats.
 Includes fair image rotation and server-driven sleep scheduling.
