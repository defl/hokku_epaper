# TODO

Items captured during development that aren't urgent enough to block current
work but worth picking up later.

v3.1
 - screen log push to webserver in POST, UI in screen (backwards compatible protocol)
 - force screens to only show images in their orientation
 - white border optimizer
 - linting auto-formatting etc
 - overhaul for defensive coding

v4.0 - The Appliance
 - This is a single Pi computer that can server images as well as flash
 - This has a access point mode that people can connect to and configure and then it boots up in client mode
 - Ships as an image that people can easily flash: pack firmware in deb, check if screen has right firmware, if not warn. OTA update?
 - This must be able to flash the screen as well
 - Start a small shop that sells these as a kit, pre-flashed, such that it's very easy to get started
 
 v4.1
 - Gift mode, installer choice multi wifi use
