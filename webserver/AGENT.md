# Agent instructions

Do **not** change `webserver/webserver/dither.py` or any of its dedicated unit tests unless the user has given **explicit permission** in the current conversation for that edit.

**In scope for this rule (non-exhaustive):**

- `webserver/webserver/dither.py`
- `webserver/tests/test_dither.py`
- `webserver/tests/test_dither_functions.py`
- `webserver/tests/test_dither_fixture_images.py`

Do **not** change `webserver/webserver/config.py` or `webserver/tests/test_config.py` unless the user has given **explicit permission** in the current conversation for that edit.

Other code (e.g. `image.py`, `flask_app.py`) may still be edited as needed unless the user says otherwise.
